import { IfCustomerDirective } from './if-customer.directive';

describe('IfCustomerDirective', () => {
  it('should create an instance', () => {
    const directive = new IfCustomerDirective();
    expect(directive).toBeTruthy();
  });
});
